{
  "name": "air-quality-ingestion-managed",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:api": "node scripts/check-reading-api.mjs",
    "test": "tsx --test tests/*.test.ts",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/src/server.js",
    "vercel-build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/helmet": "^13.0.1",
    "convex": "^1.42.3",
    "dotenv": "^16.6.1",
    "fastify": "^5.6.0",
    "pino": "^9.9.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
